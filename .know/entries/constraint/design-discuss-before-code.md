# 视觉/交互变更必须先讨论形态再实现

## Rule
涉及 hover 效果、动画、图标展示、布局形态的任务，不能直接写代码。
必须先列出 2-3 个方案让用户选择，确认后再实现。

## Why
用户两次要求回退已实现的代码重新讨论：
1. 第一次：响应式方案跳过 brainstorm 直接用 !important 覆盖字号，不生效，回退
2. 第二次：搜索 hover 特效和快捷键提示直接实现，风格不符，回退

## How to check
Sprint evaluate 时，凡是涉及视觉/交互的任务，design=yes。
即使看起来"实现方式唯一"，也要先提方案对比。
