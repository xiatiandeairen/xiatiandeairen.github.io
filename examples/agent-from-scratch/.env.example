# Optional. Without it, every stage falls back to a deterministic offline mock
# LLM so the mechanics still run (no network, no key required). Set it to drive
# the same loops with a real model.
ANTHROPIC_API_KEY=

# Optional model override (default: claude-sonnet-4-6).
AGENT_MODEL=claude-sonnet-4-6
